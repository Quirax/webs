{
    "type": "module",
    "dependencies": {
        "child_process": "^1.0.2",
        "dotenv": "^16.0.2",
        "dotenv-expand": "^9.0.0",
        "express": "^4.18.1",
        "mongoose": "^6.6.1",
        "node-pre-gyp": "^0.17.0",
        "socket.io": "^4.5.0",
        "wrtc": "^0.4.7"
    },
    "scripts": {
        "start": "cross-env NODE_ENV=development nodemon -w .env -w .env.development -w .env.local -w .env.development.local -w . .",
        "test": "cross-env NODE_ENV=test nodemon -w .env -w .env.test -w .env.local -w .env.development.local -w . ."
    }
}
